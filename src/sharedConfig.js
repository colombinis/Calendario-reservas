// src/sharedConfig.js
// Centralized configuration for shared API endpoints and constants

const SHAREDCONFIG = {
  api_auth_login: '/api/auth/login',
  api_auth_register: '/api/auth/register',
  // Add more endpoints as needed
};

module.exports = { SHAREDCONFIG };
